# MongoDB Atlas connection string — required for bookings to save.
# Get this from your Atlas cluster: Connect > Drivers.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/nuga-best-ruai

# Admin password for /admin page. Required for booking management access.
ADMIN_PASSWORD=your-secret-password-here
